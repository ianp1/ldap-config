$TTL 1D
@ IN  SOA {{ item }}. ns.{{ item }}. (
      {{ '%Y%m%d' | strftime }}{{ bind9count }}   ; Serial
      28800   ; Refresh
      7200    ; Retry
      2419200 ; Expire
      10800 )   ;TTL Negative Cache
@ IN  NS  {{ item }}.
@ IN  A   {{ dns_target_ip }}
